Dialog: Lager überschreiben
===========================

Dieser Dialog wird über das QML Item `StorageView.qml` aufgerufen und ist in der
Datei `StorageDialog.qml` implementiert:

.. literalinclude:: ../../src/view/StorageDialog.qml
   :language: javascript
   :linenos: