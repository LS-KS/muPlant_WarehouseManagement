StackLayoutView
===============

Dieses layout wird in `main.qml` im unteren Bereich als Tabulartor- oder Registeransicht verwendet. 
Der Benutzer kann auf Knopfdruck zwischen den drei Layouts `CommissionView`, `EventView` oder 
`Inventoryview.qml` wechseln. Das Layout is in der Datei `StackLayoutView.qml` implementiert:

.. literalinclude:: ../../src/view/StackLayoutView.qml
   :language: javascript
   :linenos: