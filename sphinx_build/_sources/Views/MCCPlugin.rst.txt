Manual Commission Control
=========================

Das Plugin um manuelle Kommissionsaufträge zu erzeugen wird in der `main.qml` erzeugt, sobald die entsprechende Action ausgeführt wird. 
Das GUI des PlugIns ist in der Datei `MCCPlugin.qml` implementiert:

.. literalinclude:: ../../src/view/MCCPlugin.qml
   :language: javascript
   :linenos: